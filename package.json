{
  "name": "hawiyat-agents-benchmarks",
  "version": "1.0.0",
  "private": true,
  "description": "Mono-repo of independent AI agent benchmark suites",
  "scripts": {
    "setup": "pnpm install",
    "swe-bench": "cd benchmarks/swe-bench && pnpm setup",
    "test": "echo 'Run tests inside each benchmark directory'"
  },
  "packageManager": "pnpm@9.0.0"
}
