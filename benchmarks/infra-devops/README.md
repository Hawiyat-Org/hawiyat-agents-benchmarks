# Infrastructure / DevOps Agent Benchmark

Coming soon.

## Planned Challenges

- Terraform misconfigurations (public S3 buckets, open security groups)
- Docker container vulnerabilities (running as root, exposed secrets)
- Kubernetes YAML errors (missing resource limits, privileged containers)
- CI/CD pipeline failures (broken GitHub Actions, missing env vars)
- Nginx / reverse proxy misconfigurations
- Database migration failures
- Environment variable leaks
- Log rotation and monitoring setup
- SSL/TLS certificate issues
- Infrastructure as Code drift detection

## Agent Task

Diagnose infrastructure issues, fix Terraform/Docker/K8s configs, and ensure deployments succeed.
